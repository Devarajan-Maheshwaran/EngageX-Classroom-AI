// confusionTracker.js — retained for backward compatibility
// Confusion detection logic has moved to:
//   analyticsService.js  → sliding window (recentWindow)
//   engagementService.js → spike detection (Phase 3B)
module.exports = {};
